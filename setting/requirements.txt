# ============================================================
#  aiu-agent 전체 의존성 (install 시 한 줄씩 순차 설치)
#  무거운 패키지(mlflow, deepagents)는 서브 의존성 뒤에 배치
# ============================================================

# CLI UI (로고, 하단 상태바)
rich
prompt_toolkit

# ML 기반 패키지 (mlflow/scikit-learn의 서브 의존성 — 먼저)
numpy
pandas
joblib
scikit-learn

# 실험 추적 (numpy/pandas/scikit-learn 설치 후)
mlflow

# LangChain (deepagents 서브 의존성 — 먼저)
langchain-openai
langchain-anthropic

# 로컬 서빙
fastapi
uvicorn

# 추론 요청 (Endpoint 테스트)
requests

# 에이전트 구동 (맨 마지막 — 위 의존성 모두 설치 후)
deepagents
