# UI Builder

> `agent_aiu_custom` 기반 브랜치 — UI 빌더 개발용

## 개요
AI-Studio 포탈의 **2.1 UI 빌더** 구현 브랜치.

일반 사용자가 폼(Form)으로 에이전트를 만드는 화면.

## 관련 브랜치
- `agent_aiu_custom` : 에이전트 엔진 본체
- `ui-builder` : 이 브랜치 (UI 빌더 개발)
