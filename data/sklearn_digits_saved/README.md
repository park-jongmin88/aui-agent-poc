# sklearn_digits_saved

이미 학습된 sklearn 모델(`svm_model.pkl`)이 들어있는 샘플.
학습 스크립트 없이 **저장된 모델만** 있는 경우 (분석기의 "모델 파일 존재" 케이스).

- svm_model.pkl : digits 데이터로 학습한 SVC 모델
- predict_example.py : 저장된 모델 로드 + 추론 예시
